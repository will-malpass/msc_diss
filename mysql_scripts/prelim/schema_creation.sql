CREATE SCHEMA `rdd_rob` ;  /*for hosting tables for generating robustness test data */
CREATE SCHEMA `tallies` ;  /*for hosting tables for counts (account worth, new/existing/total friends etc...*/
CREATE SCHEMA `networks` ; /*for hosting tables for generating social networks*/
CREATE SCHEMA `in_means` ; /*for hosting tables for generating the average group outcome*/
CREATE SCHEMA `exports` ;  /*for hosting tables ready for csv export*/
CREATE SCHEMA `sdb` ;      /*for hosting the steam dataset, leave this untouched*/

